---
- hosts: webservers
  become_user: true
  tasks:
    - name: Install latest version of Apache
      shell: sudo mkdir sample3
    - name: start apache server
      service:
      name: httpd
      state: started
      enabled: True
    - name: Deploy Static Website
      copy:
      src: index.html
      dest: /var/www/html/
